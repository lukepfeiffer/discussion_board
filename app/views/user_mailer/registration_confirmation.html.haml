#confirm_email
  %p.username= "Hi " + @user.username
  .body
    %p Thanks for registering! To confirm registration click the link below.

    %p= link_to confirm_email_user_url(@user.confirm_token), confirm_email_user_url(@user.confirm_token)
    %p Not you? Ignore this email.
